services:
  ruby:
    build:
      context: .
      target: base
    volumes:
      - .:/app 
      - rubygems:/usr/local/bundle
    working_dir: /app

  api01: &api
    build:
      context: .
      target: base
    container_name: api01
    hostname: api01
    environment:
      - REDIS_URL=redis://redis:6379/0
    volumes:
      - .:/app 
      - rubygems:/usr/local/bundle
    working_dir: /app
    networks:
      - payment-processor
      - backend
    command: ruby server.rb
    deploy:
      resources:
        limits:
          cpus: '0.4'
          memory: '100MB'

  api02: 
    <<: *api
    container_name: api02
    hostname: api02
    deploy:
      resources:
        limits:
          cpus: '0.4'
          memory: '100MB'

  worker:
    <<: *api
    container_name: worker
    hostname: worker
    command: bundle exec sidekiq -r ./app/payment_job.rb -c 10
    depends_on:
      - redis
    deploy:
      resources:
        limits:
          cpus: '0.4'
          memory: '100MB'

  redis:
    image: redis:7-alpine
    container_name: redis
    hostname: redis
    ports:
      - 6379:6379
    networks:
      - backend
    deploy:
      resources:
        limits:
          cpus: '0.2'
          memory: '40MB'

  nginx:
    image: nginx 
    container_name: nginx
    volumes:
      - ./config/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - 9999:9999
    networks:
      - backend
    depends_on:
      - redis
      - api01
      - api02
      - worker
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: '10MB'

volumes:
  rubygems:
networks:
  payment-processor:
    external: true
  backend:
