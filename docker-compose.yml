services:
  api01: &api
    build:
      context: .
      target: base
    container_name: api01
    hostname: api01
    environment:
      - REDIS_URL=redis://redis:6379/0
    volumes:
      - .:/app 
      - rubygems:/usr/local/bundle
    working_dir: /app
    networks:
      - payment-processor
      - backend
    command: ruby server.rb

  api02: 
    <<: *api
    container_name: api02
    hostname: api02

  sidekiq:
    <<: *api
    container_name: sidekiq
    hostname: sidekiq
    command: bundle exec sidekiq -r ./app/payment_job.rb
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    container_name: redis
    hostname: redis
    ports:
      - 6379:6379
    networks:
      - backend

  nginx:
    image: nginx 
    container_name: nginx
    volumes:
      - ./config/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - 9999:9999
    networks:
      - backend
    depends_on:
      - redis
      - api01
      - api02
      - sidekiq

volumes:
  rubygems:
networks:
  payment-processor:
    external: true
  backend:
