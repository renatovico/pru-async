services:
  api01: &api
    build:
      context: .
      target: base
    container_name: api01
    hostname: api01
    environment:
      - REDIS_URL=redis://redis:6379/0
    volumes:
      - .:/app 
      - rubygems:/usr/local/bundle
    working_dir: /app
    networks:
      - payment-processor
      - backend
    command: ruby server.rb
    deploy:
      resources:
        limits:
          cpus: '0.2'
          memory: '30MB'

  api02: 
    <<: *api
    container_name: api02
    hostname: api02
    deploy:
      resources:
        limits:
          cpus: '0.2'
          memory: '30MB'

  sidekiq01:
    <<: *api
    container_name: sidekiq01
    hostname: sidekiq01
    command: bundle exec sidekiq -r ./app/payment_job.rb -c 5
    depends_on:
      - redis
    deploy:
      resources:
        limits:
          cpus: '0.4'
          memory: '115MB'

  sidekiq02:
    <<: *api
    container_name: sidekiq02
    hostname: sidekiq02
    command: bundle exec sidekiq -r ./app/payment_job.rb -c 5
    depends_on:
      - redis
    deploy:
      resources:
        limits:
          cpus: '0.4'
          memory: '115MB'

  redis:
    image: redis:7-alpine
    container_name: redis
    hostname: redis
    ports:
      - 6379:6379
    networks:
      - backend
    deploy:
      resources:
        limits:
          cpus: '0.2'
          memory: '50MB'

  nginx:
    image: nginx 
    container_name: nginx
    volumes:
      - ./config/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - 9999:9999
    networks:
      - backend
    depends_on:
      - redis
      - api01
      - api02
      - sidekiq01
      - sidekiq02
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: '10MB'

volumes:
  rubygems:
networks:
  payment-processor:
    external: true
  backend:
